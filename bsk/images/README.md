# BSK Builders — gallery photos

Drop the job photos in this folder with these exact filenames (referenced by `../index.html`):

| Filename | Caption on site |
|---|---|
| `kitchen-refit-1.jpg` | Full kitchen refit (wide feature tile) |
| `kitchen-refit-2.jpg` | Flooring & tiling |
| `kitchen-refit-3.jpg` | Units & worktops (tall tile) |
| `kitchen-refit-4.jpg` | Full kitchen refit |
| `kitchen-in-progress.jpg` | Work in progress |

To add more photos later, copy one of the `<figure class="gal-item">` blocks in `index.html` and point it at a new file here.

Before committing any photo: compress anything over ~300KB to max 1200px-wide JPEG. Never embed images as base64.
